% Generate sample data
x = 0:10000; % X-axis values
y = 0:10000; % Y-axis values (random)

% Plot the line chart
plot(x, y);

% Add labels and title
xlabel('No. of bits shift');
ylabel('Linear complexity');
title('Line Chart ');

% Optionally, add grid lines
grid on;
